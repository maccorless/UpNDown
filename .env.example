# Root-level examples used by deployment commands
# Client points at the deployed realtime server.
VITE_SOCKET_URL=https://upndown-server.example.com

# Server network config.
PORT=3001
NODE_ENV=production
ALLOWED_ORIGINS=https://upndown-client.example.com
